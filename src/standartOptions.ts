import { Options } from './interfaces';

const standartOptions: Options = {
  orientation: 'horizontal',
  type: 'double',
  min: 0,
  max: 10,
  step: 1,
  from: 2,
  to: 6,
  hideFromTo: false,
  hideScale: false,
};

export { standartOptions };
